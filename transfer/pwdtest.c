#include <stdio.h>#include <fcntl.h>#include <sgtty.h>#include <modes.h>#include <stat.h>#define O_RDONLY         00#define O_WRONLY         01#define O_RDWR           02#include "getcwd.c"void main (argc, argv)int argc;char **argv;{    int i;    char *cwd;    char tempbuf[MAXPATHLEN];    for(i = 0; i < 4; ++i)    {        cwd = getcwd(tempbuf, MAXPATHLEN);        printf("%4d - %s\n", i, cwd);    }}